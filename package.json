{
  "name": "webpacker",
  "version": "0.1.0",
  "scripts": {
    "test": "cd packages/example && yarn build"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true, 
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
